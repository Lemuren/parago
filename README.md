# Parago

Parago is a DAG job orchestrator written in Go.
It allows you define jobs with dependencies, and execute them in parallel.

Simply supply a workflow YAML and Parago will parallelize the tasks
as much as possible.

```sh
parago workflow.yaml
```

# Building

# Testing
